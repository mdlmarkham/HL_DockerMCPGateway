{
  "mcpServers": {
    "markitdown": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "mcp-markitdown",
        "markitdown-mcp"
      ],
      "transport": "stdio"
    }
  }
}
